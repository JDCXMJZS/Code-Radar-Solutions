#include <stdio.h>
int main() {
    int a,x;
    x>a;
    scanf("%d",&a);
    if(a%x==0 && a>1){
        printf("Not Prime");
    }
    else{
        printf("Prime");
    }
    return 0;
}